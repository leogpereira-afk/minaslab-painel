import { ChevronRight, X } from "lucide-react";
import { nomeMovimento } from "../../lib/movimentoNome.js";

const moeda=(v)=>Number(v||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
const dataBR=(v)=>{if(!v)return "—";const p=String(v).slice(0,10).split("-");return p.length===3?`${p[2]}/${p[1]}/${p[0]}`:v};
export function MovimentoLinha({m,onClick}){const entrada=String(m.tipo||"").toUpperCase()==="CREDITO";return <tr onClick={onClick} className="cursor-pointer border-t transition hover:bg-slate-50"><td className="px-4 py-3 whitespace-nowrap">{dataBR(m.data_movimento)}</td><td className="px-4 py-3"><div className="font-semibold text-slate-800">{nomeMovimento(m)}</div></td><td className={`px-4 py-3 text-right font-semibold whitespace-nowrap ${entrada?"text-emerald-700":"text-rose-700"}`}>{entrada?"+ ":"- "}{moeda(Math.abs(Number(m.valor||0)))}</td><td className="px-3 py-3 text-right"><ChevronRight size={17} className="inline text-slate-400"/></td></tr>}
export function MovimentoPainel({m,onClose}){if(!m)return null;const entrada=String(m.tipo||"").toUpperCase()==="CREDITO";return <div className="fixed inset-0 z-50 flex justify-end bg-slate-950/25" onClick={onClose}><aside className="h-full w-full max-w-lg overflow-y-auto bg-white p-6 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Detalhes da movimentação</p><h2 className="mt-1 text-xl font-bold text-slate-950">{nomeMovimento(m)}</h2></div><button className="rounded-lg p-2 hover:bg-slate-100" onClick={onClose}><X size={20}/></button></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-3"><span className="text-xs text-slate-500">Data</span><p className="font-semibold">{dataBR(m.data_movimento)}</p></div><div className="rounded-xl bg-slate-50 p-3"><span className="text-xs text-slate-500">Valor</span><p className={`font-bold ${entrada?"text-emerald-700":"text-rose-700"}`}>{entrada?"Entrada · ":"Saída · "}{moeda(Math.abs(Number(m.valor||0)))}</p></div></div><div className="mt-5 space-y-4 text-sm"><div><p className="mb-1 text-xs font-semibold uppercase tracking-wide text-slate-500">Descrição completa do banco</p><p className="whitespace-pre-wrap break-words rounded-xl border bg-slate-50 p-3 text-slate-700">{m.descricao||"Sem descrição"}</p></div>{m.documento&&<div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Documento</p><p className="mt-1 break-all">{m.documento}</p></div>}{m.fitid&&<div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Identificador bancário</p><p className="mt-1 break-all">{m.fitid}</p></div>}<div className="grid grid-cols-2 gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Origem</p><p className="mt-1">{m.origem||"Manual"}</p></div><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Situação</p><p className="mt-1">{m.conciliado||m.classificacao_bancaria?"Resolvido":"A conferir"}</p></div></div></div></aside></div>}
