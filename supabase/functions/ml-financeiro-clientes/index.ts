import "jsr:@supabase/functions-js/edge-runtime.d.ts";import{createClient}from"https://esm.sh/@supabase/supabase-js@2";const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};const json=(b:any,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,"Content-Type":"application/json"}});const digits=(v:any)=>String(v||"").replace(/\D/g,"");async function auth(req:Request){const t=(req.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!t)return null;const p=t.split(".");if(p.length!==3)return null;try{const x=JSON.parse(atob(p[1].replace(/-/g,"+").replace(/_/g,"/")));if(x.exp&&x.exp*1000<Date.now())return null;return x}catch{return null}}Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});if(!await auth(req))return json({erro:"Sessão inválida."},401);const sb=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);try{const b=await req.json();if(b.action==="listar"){const{data,error}=await sb.from("clientes_financeiro").select("*").order("nome").limit(2000);if(error)throw error;const s=String(b.busca||"").toLowerCase().trim();return json({itens:s?(data||[]).filter((x:any)=>[x.nome,x.nome_fantasia,x.cnpj_cpf].some(v=>String(v||"").toLowerCase().includes(s))):data||[]})}if(b.action==="salvar"){const r=b.registro||{};if(!String(r.nome||"").trim())return json({erro:"Nome/Razão Social é obrigatório."},400);const doc=digits(r.cnpj_cpf);const{data:todos}=doc?await sb.from("clientes_financeiro").select("*").not("cnpj_cpf","is",null):{data:[]};const existente=(todos||[]).find((x:any)=>digits(x.cnpj_cpf)===doc)||null;const id=r.id||existente?.id;const payload={nome:String(r.nome).trim(),nome_fantasia:r.nome_fantasia||null,cnpj_cpf:r.cnpj_cpf||null,inscricao_estadual:r.inscricao_estadual||null,inscricao_municipal:r.inscricao_municipal||null,email:r.email||null,telefone:r.telefone||null,cep:r.cep||null,logradouro:r.logradouro||null,numero:r.numero||null,complemento:r.complemento||null,bairro:r.bairro||null,cidade:r.cidade||null,uf:r.uf||null,origem:existente?.origem||r.origem||"MANUAL",id_omie:existente?.id_omie||r.id_omie||null,usa_minaslab:Boolean(existente?.usa_minaslab||r.usa_minaslab),usa_mlab:Boolean(existente?.usa_mlab||r.usa_mlab),ativo:r.ativo!==false,updated_at:new Date().toISOString()};const res=id?await sb.from("clientes_financeiro").update(payload).eq("id",id).select().single():await sb.from("clientes_financeiro").insert(payload).select().single();if(res.error)throw res.error;return json({item:res.data,reutilizado:Boolean(existente)})}return json({erro:"Ação inválida."},400)}catch(e){return json({erro:e instanceof Error?e.message:String(e)},500)}});